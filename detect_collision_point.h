#ifndef DETECT_COLLISION_POINT
#define DETECT_COLLISION_POINT

bool detect_collision_point(const int aricraft[], const int obstacle[], const int aircraft_vel[], const int obstacle_vel[], int collision_point[], bool *ray);

#endif
